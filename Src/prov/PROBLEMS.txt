1) Возможно возвращение строк нулевой длины
в not-NULL  столбцах, некоторые клиенты считают их
NULL значениями (это в not-NULL  столбцах)  -> CRASH
2) Возможно возращение строк длины большей,
чем декларированная длина столбца (describe table)
3) По спецификации OLE DB IDBSchemaRowset должен возвращать
   информацию о доступных текущему пользователю БД и таблицах,
   сейчас IDBSchemaRowset возвращает ВСЮ метаинформацию и 
   только root'у (т.к. в MySQL C API нельзя получить код ошибки Access denied)
4) Типы 